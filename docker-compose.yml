volumes:
  pgdata:

services:
  postgresql:
    container_name: acessoria-sql
    restart: unless-stopped
    build: .
    tty: true
    stdin_open: true
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5433:5432'
    env_file:
      - .env